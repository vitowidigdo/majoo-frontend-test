export const api = `https://jsonplaceholder.typicode.com`;

const endpoint = {
  // Posts
  posts: `${api}/posts`,

  comments: `${api}/comments`,

  albums: `${api}/albums`,

  photos: `${api}/photos`,

  todos: `${api}/todos`,

  users: `${api}/users`,
};

export default endpoint;
